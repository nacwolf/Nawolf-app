kind = "web"
previewPath = "/"
title = "Kostr"
version = "1.0.0"
id = "artifacts/kostr"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24859

[services.development]
run = "pnpm --filter @workspace/kostr run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kostr", "run", "build" ]
publicDir = "artifacts/kostr/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24859"
BASE_PATH = "/"
